# Schemas

This directory will hold schema definitions for generated artifacts such as:
- wallet swaps
- paired trades
- fee and tip scans
- latency simulation rows
- entry context enrichment

Each schema should be versioned when field meaning changes.
